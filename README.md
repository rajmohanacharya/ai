# RajAIAssist GitHub Pages v2.9

Upload this directory's contents to the root of `rajmohanacharya/ai`.

v2.9 adds:

- Ubuntu local font stack;
- Classic, AMD-inspired, Oracle-inspired, and Garuda-inspired themes;
- 1K/2K/4K/8K output selection;
- Balanced, Conservative, and Evidence-only reliability modes;
- runtime, context, output, elapsed, and compute badges;
- clipboard paste and drag/drop screenshot OCR ingestion;
- Continue response action;
- owner/guest Memory Vault UI and existing prompt libraries.

When a runtime is online, preserve its current `runtime-endpoint.json` or run `rajai-colab repair` after deployment.
