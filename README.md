# RajAIAssist GitHub Pages v2.8

Upload this directory's contents to the root of `rajmohanacharya/ai`.

The page adds:

- endpoint-manifest schema v2 with owner and guest signatures;
- owner-only Personal Memory Vault controls, including explicit chat-to-inbox suggestions and approve/reject actions;
- 20 advanced Documents & Knowledge prompts;
- current multiple-file ingestion, coding prompts, observability prompts, favicon and social metadata.

When a runtime is online, preserve its current `runtime-endpoint.json` or republish after deployment.
